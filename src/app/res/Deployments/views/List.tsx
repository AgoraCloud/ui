import { WorkspaceModel } from 'app/res/Workspaces/models';
import { DeploymentsModel } from '../models';

export const DeploymentsList = ({
  workspace,
}: {
  workspace: WorkspaceModel;
}) => {
  return null;
};
